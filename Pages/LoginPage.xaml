<Page x:Class="WpfApp_DataBinding_Ver2.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp_DataBinding_Ver2.Pages"
      xmlns:main ="clr-namespace:WpfApp_DataBinding_Ver2"
      mc:Ignorable="d" 
      d:DesignHeight="270" d:DesignWidth="400"
      Title="LoginPage">

    <Page.Resources>
        <main:User x:Key="UserLog"/>
    </Page.Resources>
    
    <Grid>
        <Border DataContext="{StaticResource UserLog}" Style="{StaticResource SpecBorder}">
            <StackPanel Style="{StaticResource Panels}">
                <DockPanel HorizontalAlignment="Center">
                    <Label Content="Identificaror" DockPanel.Dock="Top"  HorizontalAlignment="Center"/>
                    <TextBox Height="25" HorizontalAlignment="Stretch" MinWidth="100" Text="{Binding ID}"/>
                </DockPanel>
                <DockPanel HorizontalAlignment="Center">
                    <Label Content="Password" DockPanel.Dock="Top" HorizontalAlignment="Center"  />
                    <TextBox Height="25" HorizontalAlignment="Stretch" MinWidth="100" Text="{Binding Password}"/>
                </DockPanel>
                <Button Content="Войти" Height="40" Width="300"  HorizontalAlignment="Center" Click="Login"/>
                <Button Content="Регистрация" Height="40" Width="300"  HorizontalAlignment="Center" Click="Gotoregister"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
