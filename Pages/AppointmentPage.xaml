<Page x:Class="WpfApp_DataBinding_Ver2.Pages.AppointmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp_DataBinding_Ver2.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="570" d:DesignWidth="1000"
      Title="AppointmentPage" Background="White">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border BorderBrush="Black" BorderThickness="1" Margin="5">
            <StackPanel Margin="10">
                <Label Content="Name"/>
                <Label Content="{Binding Name}" Margin="0,5" HorizontalAlignment="Left"/>
                <Label Content="Surname"/>
                <Label Content="{Binding Surname}" Margin="0,5" HorizontalAlignment="Left"/>
                <Label Content="Lastname"/>
                <Label Content="{Binding Lastname}" Margin="0,5" HorizontalAlignment="Left"/>
                <Label Content="Birthday"/>
                <Label Content="{Binding Birthday, StringFormat='Date of birthday: {0:dd.MM.yyyy}'}" Margin="0,5" HorizontalAlignment="Left"/>

                <DockPanel Margin="0,5" LastChildFill="True">
                    <Label Content="New diagnosis:" DockPanel.Dock="Top" Margin="0,0,0,5" HorizontalAlignment="Center"/>
                    <TextBox Height="30" HorizontalAlignment="Stretch" x:Name="nDiagnosis"/>
                </DockPanel>

                <DockPanel Margin="0,5" LastChildFill="True" DataContext="{Binding Appointmentstories}">
                    <Label Content="Recomindations:" DockPanel.Dock="Top" Margin="0,0,0,5" HorizontalAlignment="Center"/>
                    <TextBox Height="76" HorizontalAlignment="Stretch" x:Name="nRecomendations"/>
                </DockPanel>
                <Button Width="300" Height="35" Content="Закончить прием" HorizontalAlignment="Center" Margin="0,10" Click="EndReception"/>
                <Button Width="300" Height="35" Content="Назад" HorizontalAlignment="Center" Margin="0,10" Click="Back"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" BorderBrush="Black" BorderThickness="1" Margin="5">
            <ListView ItemsSource="{Binding Appointmentstories}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5">
                            <TextBlock Text="{Binding Date, StringFormat='Дата: {0:dd.MM.yyyy}'}" />
                            <TextBlock Text="{Binding Diagnosis}"/>
                            <TextBlock Text="{Binding Recommendations}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>
