<Page x:Class="WpfApp_DataBinding_Ver2.Pages.RegisterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp_DataBinding_Ver2.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="465" d:DesignWidth="800"
      Title="RegisterPage" Background="White">

    <Grid>
        <Border BorderBrush="Black" BorderThickness="1" Margin="0,0,0,5" >
            <StackPanel Margin="10">
                <DockPanel HorizontalAlignment="Center">
                    <Label Content="Name" DockPanel.Dock="Top" Margin="0,0,0,5" HorizontalAlignment="Center"/>
                    <TextBox Height="25" HorizontalAlignment="Stretch" MinWidth="100" Text="{Binding Name}"/>
                </DockPanel>
                <DockPanel HorizontalAlignment="Center">
                    <Label Content="Surname" DockPanel.Dock="Top" Margin="0,0,0,5" HorizontalAlignment="Center"/>
                    <TextBox Height="25" HorizontalAlignment="Stretch" MinWidth="100" Text="{Binding Surname}"/>
                </DockPanel>
                <DockPanel HorizontalAlignment="Center">
                    <Label Content="Lastname" DockPanel.Dock="Top" Margin="0,0,0,5" HorizontalAlignment="Center"/>
                    <TextBox Height="25" HorizontalAlignment="Stretch" MinWidth="100" Text="{Binding Lastname}"/>
                </DockPanel>
                <DockPanel HorizontalAlignment="Center" >
                    <Label Content="Specialization" DockPanel.Dock="Top" Margin="0,0,0,5" HorizontalAlignment="Center"/>
                    <TextBox Height="25" HorizontalAlignment="Stretch" MinWidth="100" Text="{Binding Specialization}"/>
                </DockPanel>
                <DockPanel HorizontalAlignment="Center" >
                    <Label Content="Password" DockPanel.Dock="Top" Margin="0,0,0,5" HorizontalAlignment="Center"/>
                    <TextBox Height="25" HorizontalAlignment="Stretch" MinWidth="100" Text="{Binding Password}"/>
                </DockPanel>
                <DockPanel HorizontalAlignment="Center" >
                    <Label Content="Password confirmation" DockPanel.Dock="Top" Margin="0,0,0,5" HorizontalAlignment="Center"/>
                    <TextBox Height="25" HorizontalAlignment="Stretch" MinWidth="100" Text="{Binding Passwordconfirm}"/>
                </DockPanel>
                <Button Content="Зарегестрироваться" Height="40" Width="300" Margin="0,10,0,0" HorizontalAlignment="Center" Click="Registration"/>
                <Button Content="Войти" Height="40" Width="300" Margin="0,10,0,0" HorizontalAlignment="Center" Click="Back"/>
            </StackPanel>
        </Border>

    </Grid>
</Page>
