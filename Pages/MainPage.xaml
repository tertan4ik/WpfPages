<Page x:Class="WpfApp_DataBinding_Ver2.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp_DataBinding_Ver2.Pages"
      xmlns:main="clr-namespace:WpfApp_DataBinding_Ver2"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage" Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel x:Name="UserInfo">
            <Border BorderBrush="Black" BorderThickness="1" Margin="5" VerticalAlignment="Top">
                <StackPanel Margin="10">
                    <Label Content="Name:"/>
                    <Label Content="{Binding Name,Mode=OneTime}" HorizontalContentAlignment="Left" Margin="0,5"/>
                    <Label Content="Surname:"/>
                    <Label Content="{Binding Surname,Mode=OneTime}" HorizontalContentAlignment="Left" Margin="0,5"/>
                    <Label Content="Lastname:"/>
                    <Label Content="{Binding Lastname,Mode=OneTime}" HorizontalContentAlignment="Left" Margin="0,5"/>
                    <Label Content="Specialization:"/>
                    <Label Content="{Binding Specialization,Mode=OneTime}" HorizontalContentAlignment="Left" Margin="0,5"/>
                    <Label Content="ID:"/>
                    <Label Content="{Binding ID,Mode=OneTime}" HorizontalContentAlignment="Left" Margin="0,5"/>
                </StackPanel>
            </Border>
            <Border BorderBrush="Black" BorderThickness="1" Margin="5"  Grid.Column="0">
                <StackPanel Margin="10">
                    <Button Content="Добавить пациента" Margin="0,5" Click="Gotoadd"/>
                    <Button Content="Изменить пациента" Margin="0,5" Click="Gotoredact"/>
                    <Button Content="Удалить пациента"  Margin="0,5" Click="DeleteUser"/>
                    <Button Content="Начать прием"  Margin="0,5" Click="GotoRecept"/>
                </StackPanel>
            </Border>
        </StackPanel>
      


        <Border BorderBrush="Black" BorderThickness="1" Grid.Column="1" Margin="5">
            <ListView Grid.Column="1" ItemsSource="{Binding Pacients}" SelectedItem="{Binding Selectedpacient}"> 
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Идентификатор: "/>
                                <TextBlock Text="{Binding ID}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Имя: "/>
                                <TextBlock Text="{Binding Name}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Возраст: "/>
                                <TextBlock Text="{Binding Surname}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Адрес электронной почты: "/>
                                <TextBlock Text="{Binding Birthday, StringFormat='Date of birthday: {0:dd.MM.yyyy}'}"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>



    </Grid>
</Page>
